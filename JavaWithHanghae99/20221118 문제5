class Solution {
    public String solution(String s) {
        // 문제 5
        // 문자열 s를 숫자로 변환한 결과를 반환하는 함수, solution을 완성하세요.
        
        int answer = 0;
        
        int num = Integer.parseInt(s);
        answer = num;
        
        return answer;
    }
}
